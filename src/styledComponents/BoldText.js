import styled from "styled-components";

const BoldText = styled.p`
  display: flex;
  align-items: center;
  font-family: "Inter";
  font-style: normal;
  font-weight: 800;
  font-size: 16px;
  line-height: 19px;
  margin: 0;
`;

export default BoldText;
